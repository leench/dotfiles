*始终使用中文进行对话*
*在未明确需求时，不要进行任何改动*
*对话保持简洁高效*
*优先保持代码局部、直接、可读。不要为单次调用、简单逻辑或尚未稳定的流程过早提取辅助方法、工具函数或封装。只有当逻辑在多个独立位置重复、需要统一关键规则，或提取后能显著提升主流程可读性时，才进行抽象；否则使用清晰的局部变量、注释和内联代码表达意图。不要仅为了减少几行代码或追求"整洁"，新增薄包装的 get_*、build_*、format_* 等方法。*

# Dotfiles 协作规范

## 环境边界

本仓库服务于多类主机，改动前先判断目标环境：
- macOS：主要本机环境，通常使用 Homebrew。
- Linux 桌面：如 Arch/Niri，涉及 Wayland、Waybar、Niri 等 GUI 配置。
- Linux 远程主机：优先保留最小 CLI 配置，避免引入桌面依赖。

涉及平台判断时，优先复用已有 `uname` / `DISPLAY` / `WAYLAND_DISPLAY` 逻辑，不要把某一台机器的假设写成全局默认。

## 目录与安装约定

- 每个一级目录视为一个可独立安装的包，例如 `tmux/`、`zsh/`、`codex/`。
- 顶层 `install.sh` 只负责按包名调度：
  - `./install.sh tmux`
  - `./install.sh zsh tmux codex`
  - `./install.sh --list`
- 某个包若有额外初始化动作，应放在该包自己的 `setup.sh` 中；不要再把包专属逻辑堆回顶层 `install.sh`。
- 需要部署的配置优先继续使用 GNU Stow 管理。

## 修改原则

- 优先做小步、可回滚、按包收敛的改动。
- 改某个工具前，先确认真实启用文件和当前符号链接，避免改到非活动副本。
- 跨平台配置要显式区分，不要让 macOS、Linux 桌面、远程服务器互相污染。
- 新增一级包时，如需要初始化逻辑，优先添加该包自己的 `setup.sh`；通常不需要修改顶层 `install.sh`。

## 敏感信息

- API Key、Token、私有主机信息不得提交到仓库。
- shell 相关敏感信息继续放在 `~/.zshrc_secret` 或其他未纳入 Git 的本机文件中。

## AI 维护提示

- 用户只要求查看或说明时，不要擅自改文件。
- 用户给出具体路径、报错或配置片段时，先围绕该证据做最小定位。
- 修改完成后，优先给出可直接执行的命令和实际影响，不要泛泛描述。
