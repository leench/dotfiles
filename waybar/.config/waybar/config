{
    "layer": "top",
    "position": "top",
    "mod": "dock",
    "exclusive": true,
    "passthrough": false,
    "gtk-layer-shell": true,
    "height": 40,
    "modules-left": ["custom/overview", "niri/workspaces", "cpu", "memory", "custom/nvidia"],
    "modules-center": ["niri/window"],
    "modules-right": ["network", "pulseaudio", "tray", "custom/theme", "clock", "custom/lock", "custom/reboot", "custom/power"],

    "custom/overview": {
        "format": " ",
        "on-click": "niri msg action toggle-overview",
        "tooltip": "Overview"
    },

    "custom/theme": {
        "format": "{}",
        "return-type": "json",
        "exec": "/home/niri/.local/bin/toggle_theme.sh",
        "on-click": "/home/niri/.local/bin/toggle_theme.sh toggle",
        "interval": 3,
        "tooltip": true
    },

    "cpu": {
        "interval": 10,
        "format": "  {usage}%",
        "max-length": 10
    },

    "memory": {
        "interval": 30,
        "format": "  {used:0.1f}G"
    },

    "custom/nvidia": {
      "exec": "nvidia-smi --query-gpu=utilization.gpu --format=csv,noheader,nounits",
      "format": "󰢮  {}%",
      "interval": 2
    },

    "network": {
      "interval": 1,
      "format": " {bandwidthDownBytes}",
      "format-ethernet": "󰱓 {bandwidthDownBytes}",
      "format-wifi": " {bandwidthDownBytes}",
      "format-linked": " {ifname} (No IP)",
      "format-disconnected": "⚠ Disconnected",
      "tooltip-format": "{ifname}: {ipaddr}"
    },

    "pulseaudio": {
        "format": "{icon}  {volume}%",
        "format-muted": "  Muted",
        "on-click": "pavucontrol",
        "scroll-step": 1,
        "format-icons": {
            "headphone": "",
            "default": ["", "", ""]
        }
    },

    "tray": {
        "spacing": 10
    },

    "clock": {
        "format": "{:%H:%M    %Y-%m-%d}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
    },

    "custom/lock": {
        "format": " ",
        "on-click": "hyprlock"
    },

    "custom/reboot": {
        "format": " ",
        "on-click": "/home/niri/.local/bin/confirm_action.sh reboot"
    },

    "custom/power": {
        "format": "⏻ ",
        "on-click": "/home/niri/.local/bin/confirm_action.sh poweroff" 
    }
}
